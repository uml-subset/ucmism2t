# README

This folder provides all files for using UCMIS M2T on the command line.

log     - all logs of script files when run
script  - folder for shell scripts
task    - folder for Ant tasks

The environment variables in script/set_env.sh must be set before running any shell scripts.